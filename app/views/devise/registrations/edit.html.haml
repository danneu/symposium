- page_header "Edit Account"

= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => "form-horizontal", :method => :put }) do |f| 

  - if resource.errors.any? 
    .alert.alert-error
      #error_explanation
        %h2
          = pluralize(resource.errors.count, "error") 
          prohibited this topic from being saved:
        %ul
          - resource.errors.full_messages.each do |msg|
            %li= msg 

  .control-group
    = f.label :email, class: "control-label"
    .controls
      = f.email_field :email
  .control-group
    = f.label :password, class: "control-label"
    .controls
      = f.password_field :password, autocomplete: "off"
      %p.help-block Leave blank if you don't want to change it.
  .control-group
    = f.label :password_confirmation, class: "control-label"
    .controls
      = f.password_field :password_confirmation 

  .control-group.well
    %h3 Confirm changes
    = f.label :current_password , class: "control-label"
    .controls
      = f.password_field :current_password

  .form-actions
    = f.submit "Update Account", class: %w[btn btn-primary]
    = link_to "Cancel", @user, class: "btn"

-#%h3 Delete my account

-#%ul
  -#%li DOES delete your username and email from the database
  -#%li DOES disassociate you from all of your posts: The author becomes "Deleted".
  -#%li DOES NOT delete your posts and topics
-#= link_to "Delete Account", registration_path(resource_name), :confirm => "Are you sure?", :method => :delete 

